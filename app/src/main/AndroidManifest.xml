<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shumin.study">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.AdminActivity" />
        <activity android:name=".ui.UserMangeActivity" />
        <activity android:name=".ui.UserEditActivity" />
        <activity android:name=".ui.QuestionManageActivity" />
        <activity android:name=".ui.ExamActivity" />
        <activity android:name=".ui.QuestionPublishActivity" />
        <activity android:name=".ui.QuestionEditActivity" />
        <activity android:name=".ui.UserActivity" />
    </application>

</manifest>
